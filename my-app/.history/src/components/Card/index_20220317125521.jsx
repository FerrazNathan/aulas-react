import React from 'react'

export default function Card({fruit}) {
  return (
    <div key={index}>
                <p>{fruit.fruit}</p>
                <p>{fruit.mixture}</p>
                </div>
  )
}
